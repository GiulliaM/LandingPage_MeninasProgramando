/**
 * Arquivo script.js
 * * Este arquivo está linkado ao index.html e é reservado para o código JavaScript
 * que irá adicionar interatividade e funcionalidades dinâmicas à Landing Page.
 * * Por enquanto, a estrutura é minimalista (MVP) e não exige JS complexo.
 * Você pode adicionar interações futuras aqui, como:
 * - Eventos de clique (ex: contadores).
 * - Efeitos visuais.
 * - Navegação responsiva (menu hambúrguer).
 */

document.addEventListener('DOMContentLoaded', () => {
    // Exemplo de código futuro: 
    // const ctas = document.querySelectorAll('.ctas-principais a');
    // ctas.forEach(cta => {
    //     cta.addEventListener('click', () => {
    //         console.log('CTA clicado!');
    //     });
    // });
});